[package]
name = "inference"
version = "0.1.0"
edition.workspace = true
publish = false

[features]
default = []
cuda = ["dep:ort"]

[dependencies]
ort = { workspace = true, optional = true }

[dev-dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
