export function IndexProgress({ phase, done, total }: { phase: string; done: number; total: number | null }) { const determinate = total !== null && total > 0; const percent = determinate ? Math.min(100, Math.round((done / total) * 100)) : 0; return <div className="progress-block"><div className="progress-meta"><span>{phase}</span><span>{done.toLocaleString()}{determinate ? ` / ${total.toLocaleString()}` : ' complete'}</span></div><div className="progress-track" aria-label={determinate ? `${percent}% complete` : 'Progress total unavailable'} role="progressbar" aria-valuemin={0} aria-valuemax={determinate ? 100 : undefined} aria-valuenow={determinate ? percent : undefined} data-indeterminate={!determinate || undefined}><span style={{ width: determinate ? `${percent}%` : undefined }} /></div>{!determinate && <p className="muted">The daemon has not reported a total; progress is continuing.</p>}</div>; }
