# tools/

One-time, non-runtime scripts (model export, verification). Not shipped with
the daemon and never imported by Rust crates.

Python packaging and dependencies arrive with SIFT-004; this directory has no
`pyproject.toml` yet.
